#!/usr/bin/env bash
set -euo pipefail
OUT_DIR="artifacts/out/baseline_oracle_only"
mkdir -p "$OUT_DIR"
echo "[Baseline] Oracle-only (no ZK) -> $OUT_DIR"
echo "[TODO] Replace this wrapper with your actual oracle-only pipeline command."
