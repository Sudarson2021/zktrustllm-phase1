#!/usr/bin/env bash
set -euo pipefail
OUT_DIR="artifacts/out/baseline_no_ipfs"
mkdir -p "$OUT_DIR"
echo "[Baseline] No-IPFS evidence (local store) -> $OUT_DIR"
echo "[TODO] Replace this wrapper with your actual no-IPFS pipeline command."
